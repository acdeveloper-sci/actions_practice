# Servicios

name: Ejemplo de Composite Action

on: [push]

jobs:
  say-hello:

    services:
      mysql:
        image: mysql:latest
        env:
          MYSQL_ROOT_PASSWORD: example
        ports:
          - 3306:3306
